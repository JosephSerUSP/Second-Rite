name: Project watcher

on:
  pull_request:
    paths:
      - package.json
      - package-lock.json
      - main.js
      - studio/editor/project-watcher.js
      - studio/editor/project-resource-invalidation.js
      - studio/editor/studio-electron.js
      - studio/editor/project-preload.js
      - studio/editor/js/studio-resource-sync.js
      - studio/editor/test-project-watcher.js
      - studio/editor/test-project-watcher-ipc.js
      - studio/editor/test-studio-resource-sync.js
      - .github/workflows/project-watcher.yml
  workflow_dispatch:

jobs:
  watcher-linux:
    name: Project watcher (Linux)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      - run: npm ci --ignore-scripts
      - run: npm run test:project-watcher

  watcher-windows:
    name: Project watcher (Windows)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      - run: npm ci --ignore-scripts
      - run: npm run test:project-watcher
      - run: npm run test:studio-session
