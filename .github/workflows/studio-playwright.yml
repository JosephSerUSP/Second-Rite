name: Studio Playwright behavior

on:
  pull_request:
    paths:
      - "main.js"
      - "package.json"
      - "package-lock.json"
      - "tools/semantic-roots.js"
      - "studio/editor/project-root.js"
      - "studio/editor/project-preload.js"
      - "studio/editor/studio-electron.js"
      - "studio/editor/studio-window-manager.js"
      - "studio/editor/studio-shutdown.js"
      - "studio/editor/project-watcher.js"
      - "studio/editor/project-resource-invalidation.js"
      - "studio/editor/js/net.js"
      - "studio/editor/js/studio-resource-sync.js"
      - "studio/editor/js/studio-surface-host.js"
      - "studio/editor/test-studio-playwright.js"
      - ".github/workflows/studio-playwright.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  editor-surface-lifecycle:
    name: EditorSurface lifecycle (Windows)
    runs-on: windows-latest
    timeout-minutes: 15

    steps:
      - name: Check out commit under test
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install pinned development dependencies
        run: npm ci

      - name: Drive real Electron EditorSurface workflow
        run: npm run test:studio-playwright

      - name: Preserve native host smoke boundary
        run: npm run test:studio-host
